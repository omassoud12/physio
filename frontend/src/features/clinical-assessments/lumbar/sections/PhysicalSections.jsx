import { BooleanChoice, ChoiceButtons, DualLabel, FindingCard, MultiCheck, NotesField, NumberField } from '../../components/AssessmentControls.jsx'
import { dynamicFindings, dynamicMovements, movementResponses, movements, palpationFindings, palpationGroups, repeatedMovements, segments, staticFindings } from '../lumbarConfig.js'
const tenderness=['0','1','2','3'].map((key)=>({key,label:key,labelAr:key}));const mobility=[{key:'hypomobile',label:'Hypomobile',labelAr:'ناقص الحركة'},{key:'normal',label:'Normal',labelAr:'طبيعي'},{key:'hypermobile',label:'Hypermobile',labelAr:'مفرط الحركة'}];const response=[{key:'better',label:'Better',labelAr:'أفضل'},{key:'worse',label:'Worse',labelAr:'أسوأ'},{key:'no_change',label:'No Change',labelAr:'لا تغيير'},{key:'centralization',label:'Centralization',labelAr:'تمركز'},{key:'peripheralization',label:'Peripheralization',labelAr:'انتشار للمحيط'}]
export function InspectionSection({value,onChange}){return <div className="assessment-section__body"><MultiCheck label="Static Inspection" labelAr="الفحص الثابت" options={staticFindings} value={value.staticFindings} onChange={(staticFindingsValue)=>onChange({...value,staticFindings:staticFindingsValue})}/><section className="clinical-subsection"><DualLabel as="h3" label="Dynamic Inspection" labelAr="الفحص الحركي"/><div className="clinical-finding-grid">{dynamicMovements.map((m)=><MultiCheck key={m.key} label={m.label} labelAr={m.labelAr} options={dynamicFindings} value={value.dynamic[m.key]||[]} onChange={(findings)=>onChange({...value,dynamic:{...value.dynamic,[m.key]:findings}})}/>)}</div></section><NotesField value={value.notes} onChange={(notes)=>onChange({...value,notes})}/></div>}
export function PalpationSection({value,onChange}){function toggle(key,active){const locations={...value.locations};if(active)locations[key]={tenderness:'',findings:[]};else delete locations[key];onChange({...value,locations})}function update(key,field,next){onChange({...value,locations:{...value.locations,[key]:{...value.locations[key],[field]:next}}})}return <div className="assessment-section__body">{palpationGroups.map((g)=><section className="clinical-subsection" key={g.label}><DualLabel as="h3" label={g.label} labelAr={g.labelAr}/><div className="clinical-finding-grid">{g.items.map((item)=>{const f=value.locations[item.key];return <FindingCard item={item} active={Boolean(f)} onToggle={(active)=>toggle(item.key,active)} key={item.key}>{f&&<div className="clinical-finding-controls"><ChoiceButtons label="Tenderness 0–3" labelAr="الإيلام 0–3" options={tenderness} value={String(f.tenderness??'')} onChange={(next)=>update(item.key,'tenderness',next===''?'':Number(next))}/><MultiCheck label="Findings" labelAr="النتائج" options={palpationFindings} value={f.findings} onChange={(next)=>update(item.key,'findings',next)}/></div>}</FindingCard>})}</div></section>)}<NotesField value={value.notes} onChange={(notes)=>onChange({...value,notes})}/></div>}
export function ArticularSection({value,onChange}){function current(key){return value.movements[key]||{rom:'',pain:null,limitation:null}}function update(key,field,next){onChange({...value,movements:{...value.movements,[key]:{...current(key),[field]:next}}})}return <div className="assessment-section__body"><section className="clinical-subsection"><DualLabel as="h3" label="Lumbar AROM" labelAr="مدى الحركة القطنية الفعّال"/><div className="rom-grid">{movements.map((m)=>{const f=current(m.key);return <article className="rom-card" key={m.key}><DualLabel as="h4" label={m.label} labelAr={m.labelAr}/><NumberField label="ROM" labelAr="مدى الحركة" min="0" max="180" suffix="°" value={f.rom} onChange={(next)=>update(m.key,'rom',next)}/><div className="rom-card__flags"><BooleanChoice label="Pain" labelAr="ألم" value={f.pain} onChange={(next)=>update(m.key,'pain',next)}/><BooleanChoice label="Limitation" labelAr="محدودية" value={f.limitation} onChange={(next)=>update(m.key,'limitation',next)}/></div></article>})}</div></section><MultiCheck label="Movement Response" labelAr="الاستجابة للحركة" options={movementResponses} value={value.movementResponse} onChange={(next)=>onChange({...value,movementResponse:next})}/><section className="clinical-subsection"><DualLabel as="h3" label="Repeated Movement Assessment" labelAr="تقييم الحركة المتكررة"/><div className="clinical-finding-grid">{repeatedMovements.map((m)=><ChoiceButtons key={m.key} label={m.label} labelAr={m.labelAr} options={response} value={value.repeatedMovements[m.key]||''} onChange={(next)=>onChange({...value,repeatedMovements:{...value.repeatedMovements,[m.key]:next}})}/>)}</div></section><section className="clinical-subsection"><DualLabel as="h3" label="Segmental Mobility" labelAr="الحركة القطعية"/><div className="segmental-mobility-grid">{segments.map((s)=><ChoiceButtons key={s.key} label={s.label} labelAr={s.labelAr} options={mobility} value={value.segmentalMobility[s.key]||''} onChange={(next)=>onChange({...value,segmentalMobility:{...value.segmentalMobility,[s.key]:next}})}/>)}</div></section><NotesField value={value.notes} onChange={(notes)=>onChange({...value,notes})}/></div>}
